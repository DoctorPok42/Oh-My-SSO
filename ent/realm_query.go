// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"sso.internal/sso/ent/auditlog"
	"sso.internal/sso/ent/clientapp"
	"sso.internal/sso/ent/clientscope"
	"sso.internal/sso/ent/group"
	"sso.internal/sso/ent/identityprovider"
	"sso.internal/sso/ent/instancesettings"
	"sso.internal/sso/ent/permission"
	"sso.internal/sso/ent/predicate"
	"sso.internal/sso/ent/realm"
	"sso.internal/sso/ent/role"
	"sso.internal/sso/ent/session"
	"sso.internal/sso/ent/signingkey"
	"sso.internal/sso/ent/timeout"
	"sso.internal/sso/ent/user"
)

// RealmQuery is the builder for querying Realm entities.
type RealmQuery struct {
	config
	ctx                   *QueryContext
	order                 []realm.OrderOption
	inters                []Interceptor
	predicates            []predicate.Realm
	withUsers             *UserQuery
	withRoles             *RoleQuery
	withGroups            *GroupQuery
	withPermissions       *PermissionQuery
	withClientScopes      *ClientScopeQuery
	withClientApps        *ClientAppQuery
	withSessions          *SessionQuery
	withSigningKeys       *SigningKeyQuery
	withAuditLogs         *AuditLogQuery
	withIdentityProviders *IdentityProviderQuery
	withInstanceSettings  *InstanceSettingsQuery
	withTimeouts          *TimeoutQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RealmQuery builder.
func (_q *RealmQuery) Where(ps ...predicate.Realm) *RealmQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RealmQuery) Limit(limit int) *RealmQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RealmQuery) Offset(offset int) *RealmQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RealmQuery) Unique(unique bool) *RealmQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RealmQuery) Order(o ...realm.OrderOption) *RealmQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUsers chains the current query on the "users" edge.
func (_q *RealmQuery) QueryUsers() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.UsersTable, realm.UsersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRoles chains the current query on the "roles" edge.
func (_q *RealmQuery) QueryRoles() *RoleQuery {
	query := (&RoleClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(role.Table, role.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.RolesTable, realm.RolesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGroups chains the current query on the "groups" edge.
func (_q *RealmQuery) QueryGroups() *GroupQuery {
	query := (&GroupClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(group.Table, group.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.GroupsTable, realm.GroupsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPermissions chains the current query on the "permissions" edge.
func (_q *RealmQuery) QueryPermissions() *PermissionQuery {
	query := (&PermissionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(permission.Table, permission.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.PermissionsTable, realm.PermissionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryClientScopes chains the current query on the "client_scopes" edge.
func (_q *RealmQuery) QueryClientScopes() *ClientScopeQuery {
	query := (&ClientScopeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(clientscope.Table, clientscope.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.ClientScopesTable, realm.ClientScopesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryClientApps chains the current query on the "client_apps" edge.
func (_q *RealmQuery) QueryClientApps() *ClientAppQuery {
	query := (&ClientAppClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(clientapp.Table, clientapp.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.ClientAppsTable, realm.ClientAppsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySessions chains the current query on the "sessions" edge.
func (_q *RealmQuery) QuerySessions() *SessionQuery {
	query := (&SessionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.SessionsTable, realm.SessionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySigningKeys chains the current query on the "signing_keys" edge.
func (_q *RealmQuery) QuerySigningKeys() *SigningKeyQuery {
	query := (&SigningKeyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(signingkey.Table, signingkey.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.SigningKeysTable, realm.SigningKeysColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAuditLogs chains the current query on the "audit_logs" edge.
func (_q *RealmQuery) QueryAuditLogs() *AuditLogQuery {
	query := (&AuditLogClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(auditlog.Table, auditlog.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.AuditLogsTable, realm.AuditLogsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryIdentityProviders chains the current query on the "identity_providers" edge.
func (_q *RealmQuery) QueryIdentityProviders() *IdentityProviderQuery {
	query := (&IdentityProviderClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(identityprovider.Table, identityprovider.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.IdentityProvidersTable, realm.IdentityProvidersColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryInstanceSettings chains the current query on the "instance_settings" edge.
func (_q *RealmQuery) QueryInstanceSettings() *InstanceSettingsQuery {
	query := (&InstanceSettingsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(instancesettings.Table, instancesettings.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, realm.InstanceSettingsTable, realm.InstanceSettingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryTimeouts chains the current query on the "timeouts" edge.
func (_q *RealmQuery) QueryTimeouts() *TimeoutQuery {
	query := (&TimeoutClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(realm.Table, realm.FieldID, selector),
			sqlgraph.To(timeout.Table, timeout.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, realm.TimeoutsTable, realm.TimeoutsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Realm entity from the query.
// Returns a *NotFoundError when no Realm was found.
func (_q *RealmQuery) First(ctx context.Context) (*Realm, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{realm.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RealmQuery) FirstX(ctx context.Context) *Realm {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Realm ID from the query.
// Returns a *NotFoundError when no Realm ID was found.
func (_q *RealmQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{realm.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RealmQuery) FirstIDX(ctx context.Context) string {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Realm entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Realm entity is found.
// Returns a *NotFoundError when no Realm entities are found.
func (_q *RealmQuery) Only(ctx context.Context) (*Realm, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{realm.Label}
	default:
		return nil, &NotSingularError{realm.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RealmQuery) OnlyX(ctx context.Context) *Realm {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Realm ID in the query.
// Returns a *NotSingularError when more than one Realm ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RealmQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{realm.Label}
	default:
		err = &NotSingularError{realm.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RealmQuery) OnlyIDX(ctx context.Context) string {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Realms.
func (_q *RealmQuery) All(ctx context.Context) ([]*Realm, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Realm, *RealmQuery]()
	return withInterceptors[[]*Realm](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RealmQuery) AllX(ctx context.Context) []*Realm {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Realm IDs.
func (_q *RealmQuery) IDs(ctx context.Context) (ids []string, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(realm.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RealmQuery) IDsX(ctx context.Context) []string {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RealmQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RealmQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RealmQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RealmQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RealmQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RealmQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RealmQuery) Clone() *RealmQuery {
	if _q == nil {
		return nil
	}
	return &RealmQuery{
		config:                _q.config,
		ctx:                   _q.ctx.Clone(),
		order:                 append([]realm.OrderOption{}, _q.order...),
		inters:                append([]Interceptor{}, _q.inters...),
		predicates:            append([]predicate.Realm{}, _q.predicates...),
		withUsers:             _q.withUsers.Clone(),
		withRoles:             _q.withRoles.Clone(),
		withGroups:            _q.withGroups.Clone(),
		withPermissions:       _q.withPermissions.Clone(),
		withClientScopes:      _q.withClientScopes.Clone(),
		withClientApps:        _q.withClientApps.Clone(),
		withSessions:          _q.withSessions.Clone(),
		withSigningKeys:       _q.withSigningKeys.Clone(),
		withAuditLogs:         _q.withAuditLogs.Clone(),
		withIdentityProviders: _q.withIdentityProviders.Clone(),
		withInstanceSettings:  _q.withInstanceSettings.Clone(),
		withTimeouts:          _q.withTimeouts.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithUsers tells the query-builder to eager-load the nodes that are connected to
// the "users" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithUsers(opts ...func(*UserQuery)) *RealmQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUsers = query
	return _q
}

// WithRoles tells the query-builder to eager-load the nodes that are connected to
// the "roles" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithRoles(opts ...func(*RoleQuery)) *RealmQuery {
	query := (&RoleClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRoles = query
	return _q
}

// WithGroups tells the query-builder to eager-load the nodes that are connected to
// the "groups" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithGroups(opts ...func(*GroupQuery)) *RealmQuery {
	query := (&GroupClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withGroups = query
	return _q
}

// WithPermissions tells the query-builder to eager-load the nodes that are connected to
// the "permissions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithPermissions(opts ...func(*PermissionQuery)) *RealmQuery {
	query := (&PermissionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPermissions = query
	return _q
}

// WithClientScopes tells the query-builder to eager-load the nodes that are connected to
// the "client_scopes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithClientScopes(opts ...func(*ClientScopeQuery)) *RealmQuery {
	query := (&ClientScopeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withClientScopes = query
	return _q
}

// WithClientApps tells the query-builder to eager-load the nodes that are connected to
// the "client_apps" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithClientApps(opts ...func(*ClientAppQuery)) *RealmQuery {
	query := (&ClientAppClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withClientApps = query
	return _q
}

// WithSessions tells the query-builder to eager-load the nodes that are connected to
// the "sessions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithSessions(opts ...func(*SessionQuery)) *RealmQuery {
	query := (&SessionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSessions = query
	return _q
}

// WithSigningKeys tells the query-builder to eager-load the nodes that are connected to
// the "signing_keys" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithSigningKeys(opts ...func(*SigningKeyQuery)) *RealmQuery {
	query := (&SigningKeyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSigningKeys = query
	return _q
}

// WithAuditLogs tells the query-builder to eager-load the nodes that are connected to
// the "audit_logs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithAuditLogs(opts ...func(*AuditLogQuery)) *RealmQuery {
	query := (&AuditLogClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAuditLogs = query
	return _q
}

// WithIdentityProviders tells the query-builder to eager-load the nodes that are connected to
// the "identity_providers" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithIdentityProviders(opts ...func(*IdentityProviderQuery)) *RealmQuery {
	query := (&IdentityProviderClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withIdentityProviders = query
	return _q
}

// WithInstanceSettings tells the query-builder to eager-load the nodes that are connected to
// the "instance_settings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithInstanceSettings(opts ...func(*InstanceSettingsQuery)) *RealmQuery {
	query := (&InstanceSettingsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withInstanceSettings = query
	return _q
}

// WithTimeouts tells the query-builder to eager-load the nodes that are connected to
// the "timeouts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RealmQuery) WithTimeouts(opts ...func(*TimeoutQuery)) *RealmQuery {
	query := (&TimeoutClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTimeouts = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Realm.Query().
//		GroupBy(realm.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RealmQuery) GroupBy(field string, fields ...string) *RealmGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RealmGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = realm.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Realm.Query().
//		Select(realm.FieldName).
//		Scan(ctx, &v)
func (_q *RealmQuery) Select(fields ...string) *RealmSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RealmSelect{RealmQuery: _q}
	sbuild.label = realm.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RealmSelect configured with the given aggregations.
func (_q *RealmQuery) Aggregate(fns ...AggregateFunc) *RealmSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RealmQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !realm.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RealmQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Realm, error) {
	var (
		nodes       = []*Realm{}
		_spec       = _q.querySpec()
		loadedTypes = [12]bool{
			_q.withUsers != nil,
			_q.withRoles != nil,
			_q.withGroups != nil,
			_q.withPermissions != nil,
			_q.withClientScopes != nil,
			_q.withClientApps != nil,
			_q.withSessions != nil,
			_q.withSigningKeys != nil,
			_q.withAuditLogs != nil,
			_q.withIdentityProviders != nil,
			_q.withInstanceSettings != nil,
			_q.withTimeouts != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Realm).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Realm{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUsers; query != nil {
		if err := _q.loadUsers(ctx, query, nodes,
			func(n *Realm) { n.Edges.Users = []*User{} },
			func(n *Realm, e *User) { n.Edges.Users = append(n.Edges.Users, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRoles; query != nil {
		if err := _q.loadRoles(ctx, query, nodes,
			func(n *Realm) { n.Edges.Roles = []*Role{} },
			func(n *Realm, e *Role) { n.Edges.Roles = append(n.Edges.Roles, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withGroups; query != nil {
		if err := _q.loadGroups(ctx, query, nodes,
			func(n *Realm) { n.Edges.Groups = []*Group{} },
			func(n *Realm, e *Group) { n.Edges.Groups = append(n.Edges.Groups, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPermissions; query != nil {
		if err := _q.loadPermissions(ctx, query, nodes,
			func(n *Realm) { n.Edges.Permissions = []*Permission{} },
			func(n *Realm, e *Permission) { n.Edges.Permissions = append(n.Edges.Permissions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withClientScopes; query != nil {
		if err := _q.loadClientScopes(ctx, query, nodes,
			func(n *Realm) { n.Edges.ClientScopes = []*ClientScope{} },
			func(n *Realm, e *ClientScope) { n.Edges.ClientScopes = append(n.Edges.ClientScopes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withClientApps; query != nil {
		if err := _q.loadClientApps(ctx, query, nodes,
			func(n *Realm) { n.Edges.ClientApps = []*ClientApp{} },
			func(n *Realm, e *ClientApp) { n.Edges.ClientApps = append(n.Edges.ClientApps, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSessions; query != nil {
		if err := _q.loadSessions(ctx, query, nodes,
			func(n *Realm) { n.Edges.Sessions = []*Session{} },
			func(n *Realm, e *Session) { n.Edges.Sessions = append(n.Edges.Sessions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSigningKeys; query != nil {
		if err := _q.loadSigningKeys(ctx, query, nodes,
			func(n *Realm) { n.Edges.SigningKeys = []*SigningKey{} },
			func(n *Realm, e *SigningKey) { n.Edges.SigningKeys = append(n.Edges.SigningKeys, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAuditLogs; query != nil {
		if err := _q.loadAuditLogs(ctx, query, nodes,
			func(n *Realm) { n.Edges.AuditLogs = []*AuditLog{} },
			func(n *Realm, e *AuditLog) { n.Edges.AuditLogs = append(n.Edges.AuditLogs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withIdentityProviders; query != nil {
		if err := _q.loadIdentityProviders(ctx, query, nodes,
			func(n *Realm) { n.Edges.IdentityProviders = []*IdentityProvider{} },
			func(n *Realm, e *IdentityProvider) { n.Edges.IdentityProviders = append(n.Edges.IdentityProviders, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withInstanceSettings; query != nil {
		if err := _q.loadInstanceSettings(ctx, query, nodes, nil,
			func(n *Realm, e *InstanceSettings) { n.Edges.InstanceSettings = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withTimeouts; query != nil {
		if err := _q.loadTimeouts(ctx, query, nodes,
			func(n *Realm) { n.Edges.Timeouts = []*Timeout{} },
			func(n *Realm, e *Timeout) { n.Edges.Timeouts = append(n.Edges.Timeouts, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RealmQuery) loadUsers(ctx context.Context, query *UserQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *User)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(user.FieldRealmID)
	}
	query.Where(predicate.User(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.UsersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadRoles(ctx context.Context, query *RoleQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *Role)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(role.FieldRealmID)
	}
	query.Where(predicate.Role(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.RolesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadGroups(ctx context.Context, query *GroupQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *Group)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(group.FieldRealmID)
	}
	query.Where(predicate.Group(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.GroupsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadPermissions(ctx context.Context, query *PermissionQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *Permission)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(permission.FieldRealmID)
	}
	query.Where(predicate.Permission(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.PermissionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadClientScopes(ctx context.Context, query *ClientScopeQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *ClientScope)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(clientscope.FieldRealmID)
	}
	query.Where(predicate.ClientScope(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.ClientScopesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadClientApps(ctx context.Context, query *ClientAppQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *ClientApp)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(clientapp.FieldRealmID)
	}
	query.Where(predicate.ClientApp(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.ClientAppsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadSessions(ctx context.Context, query *SessionQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *Session)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(session.FieldRealmID)
	}
	query.Where(predicate.Session(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.SessionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadSigningKeys(ctx context.Context, query *SigningKeyQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *SigningKey)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(signingkey.FieldRealmID)
	}
	query.Where(predicate.SigningKey(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.SigningKeysColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadAuditLogs(ctx context.Context, query *AuditLogQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *AuditLog)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(auditlog.FieldRealmID)
	}
	query.Where(predicate.AuditLog(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.AuditLogsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadIdentityProviders(ctx context.Context, query *IdentityProviderQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *IdentityProvider)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(identityprovider.FieldRealmID)
	}
	query.Where(predicate.IdentityProvider(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.IdentityProvidersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadInstanceSettings(ctx context.Context, query *InstanceSettingsQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *InstanceSettings)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(instancesettings.FieldRealmID)
	}
	query.Where(predicate.InstanceSettings(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.InstanceSettingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RealmQuery) loadTimeouts(ctx context.Context, query *TimeoutQuery, nodes []*Realm, init func(*Realm), assign func(*Realm, *Timeout)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*Realm)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(timeout.FieldRealmID)
	}
	query.Where(predicate.Timeout(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(realm.TimeoutsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.RealmID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "realm_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RealmQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RealmQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(realm.Table, realm.Columns, sqlgraph.NewFieldSpec(realm.FieldID, field.TypeString))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, realm.FieldID)
		for i := range fields {
			if fields[i] != realm.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RealmQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(realm.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = realm.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RealmGroupBy is the group-by builder for Realm entities.
type RealmGroupBy struct {
	selector
	build *RealmQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RealmGroupBy) Aggregate(fns ...AggregateFunc) *RealmGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RealmGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RealmQuery, *RealmGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RealmGroupBy) sqlScan(ctx context.Context, root *RealmQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RealmSelect is the builder for selecting fields of Realm entities.
type RealmSelect struct {
	*RealmQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RealmSelect) Aggregate(fns ...AggregateFunc) *RealmSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RealmSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RealmQuery, *RealmSelect](ctx, _s.RealmQuery, _s, _s.inters, v)
}

func (_s *RealmSelect) sqlScan(ctx context.Context, root *RealmQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
